# Stage2 提示词 / Schema / pointId 精简 TODO

日期：2026-08-08  
范围：对话分析 Stage2（语法分析）—— system prompt、user 模板、JSON Schema、Qwen 注入路径、评测与后续调参。

## 总判断

- **主因是任务过载 + 规则重复/冲突，不是 token 绝对太重。**
- Schema enum 只能保证 **合法 pointId**，不能保证 **分类准**。
- `STRUCTURE_OTHER` 过滥，与细叶子少、兜底文案偏置有关。
- **先瘦 prompt / 统一契约，再考虑二级分类或调 temperature。**

相关文件：

- `backend/src/main/resources/templates/prompts/system-prompt-conversation-analysis.txt`
- `backend/src/main/resources/templates/prompts/conversation-analysis-prompt-template.txt`
- `backend/src/main/resources/schemas/conversation-analysis-schema.json`
- `backend/src/main/resources/knowledge/point-dictionary-v1.json`
- `backend/src/main/java/com/khankiddo/learning/llm/QwenGrammarStructuredOutputPolicy.java`
- `backend/src/main/java/com/khankiddo/learning/llm/SchemaGrammarStructuredOutputPolicy.java`

---



## 刻意不做（当前）

- [ ] ~~不立刻改成二级~~ `family` ~~+~~ `pointId` ~~Schema~~（见 P2：仅当评测后仍差再开）

- 不先调模型 / temperature 当主手段
- 不大幅扩写 prompt「再教一轮分类」
- 不把完整 `point-dictionary-v1.json` 再塞进 prompt

---



## P0 — 立刻做（低风险、直接减负）

- [x] **1. 瘦身 user 模板**（2026-08-08）  
  文件：`conversation-analysis-prompt-template.txt`  
  只留：待分析内容 + `<<<UTTERANCES>>>` 边界 +「只输出符合 Schema 的 JSON」。  
  已删除：重复规则、用户/AI 分离描述、示例、pointId 字典复述。  
  目标：system prompt 作为唯一规则来源。

- [x] **2. 删掉 Stage2 已失效的「完整对话」叙事**（2026-08-08）  
  system 改为「输入已是预筛后的学习者语句」；user 不再提对话字幕 / 只分析用户不分析 AI。

- [x] **3. System prompt 加固定决策顺序（靠前、短）**（2026-08-08）  
  ```text
  1. 先判断是否是真错误；
  2. 拿不准就不要标；
  3. 确定有错误后，再选择最具体的 pointId；
  4. 最后生成 point 和 suggestion。
  ```
  原 user/schema 中的字段格式要求已迁入 system 的 `Field rules`。

- [x] **4. 精简 Schema** `description`（2026-08-08）  
  Schema 只保留：字段类型、必填、enum、`additionalProperties: false`（已去掉全部 `description`）。  
  弱化 `STRUCTURE_OTHER` 偏置：system 写明优先匹配家族 `*_OTHER`，勿默认 STRUCTURE；STRUCTURE_OTHER 仅在确认结构问题且细叶子不适用时使用。

---



## P1 — 紧接着做（分类质量）

- [ ] **5. 合并 pointId 冲突规则，只留 4 条 discriminator**  
  - 冠词 vs 复数  
  - 时态 vs 动词句型  
  - 词形 vs 整词替换（`WORD_FORM_*` vs `LEXICAL_GAP`）  
  - 介词 vs 固定搭配 vs 词汇缺口
  删除重复解释与过多示例。  
  明确：**整句润色 / 卡壳优先** `FLUENCY_`* **/** `LEXICAL_GAP`**，勿硬打** `STRUCTURE_`***。**

- [ ] **6. 统一三方契约（单一标准清单）**  
  核对并写死：  
  - `items: []` 是否允许  
  - `errors: []` 是否允许  
  - `suggestion` 是否允许空字符串  
  - `pointId` 与 `point-dictionary-v1.json` / schema enum 是否完全一致  
  - 各家族 `*_OTHER` 是否齐全可用
  改完后跑 `SchemaLoaderTest` / 字典相关测试。

- [ ] **7. Qwen 去重 Schema 注入**  
  当前：system 追加完整 Schema + JSON Mode + user 再要求遵守 Schema。  
  优先试：**去掉 system 里完整 Schema 粘贴**，保留 JSON Mode（+ 精简后 user 一句「符合 schema」）。  
  对比指标：JSON 解析失败率是否明显上升。

---



## P2 — 用评测验证（改完必跑）

- [ ] **8. 固定评测集（至少 4 类）**  
  - 明确错误  
  - 合法口语  
  - ASR 重复 / 乱码  
  - 相邻 pointId 易混
  每次改 prompt/schema 后比较：误报率、漏报率、pointId 准确率、JSON 解析失败率。  
  （可复用或扩展现有 drift harness。）

- [ ] **9. 视评测再决定是否「family → pointId」二级分类**  
  **现在不改 Schema。** 仅当精简后 `pointId` 准确率仍明显差，再开设计（会增加字段与流程复杂度）。

---



## P3 — 最后才动

- [ ] **10. Temperature 扫描**  
  Prompt / 规则稳定后测 `0.1` / `0.2` / `0.3`。  
  **不要用降温掩盖 prompt 结构问题。**

---



## 成功标准


| 指标                   | 期望方向                                     |
| -------------------- | ---------------------------------------- |
| 误报（合法口语 / ASR）       | 明显下降                                     |
| `STRUCTURE_OTHER` 占比 | 下降，且更多落到细叶子或 `FLUENCY_*` / `LEXICAL_GAP` |
| pointId 准确率          | 上升（哪怕纠句质量持平）                             |
| JSON 解析失败率           | 不显著变差（尤其 Qwen 去重后）                       |


---



## 建议落地方式

1. 先开 PR 只做 **P0（1–4）**，跑评测集对比基线。
2. 再 PR **P1（5–7）**。
3. **P2 / P3** 依数据决定，不预支 Schema 大改。

