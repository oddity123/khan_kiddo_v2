# 成长卡（Growth Cards）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 落地可持久化「成长卡」：分析完成后异步经 **LLM 生成草稿 + Gateway 显式落库** 保存 Top1 `habit` 卡（及 vocab）；提供今日到期列表与自评 API；详情可看已生成卡片。**首页只做闪卡能力展示，不做练习**；正式复习 / 复盘另开页面（后续任务）。

**Architecture:** 能力收在 `com.khankiddo.learning.growth`（**不**把逻辑散进分析 Service）。  
`GrowthCardMintAssistant`（`@AiService`，只返回 `GrowthCardDraft`，**无 Tool**）+ `GrowthCardMintGateway.mintAfterAnalysis`（generate → `store.persistNewOrGet`）。  
`persistAnalysis` 后 `AFTER_COMMIT` 虚拟线程只调 Gateway。复习/自评无 LLM，同包 `GrowthCardReviewService`。Stage3 仍只做总结。

**Tech Stack:** Java 21 · Spring Boot 3 · LangChain4j AiServices · MyBatis XML · Vue 3 · TypeScript · `vue3-flashcards`  
**Spec:** [`2026-08-02-design.md`](./2026-08-02-design.md) §6  
**构建:** 仓库根 `./mvn.sh -q test`；前端 `cd frontend && npm run build`  
**参考实现:** `ai/conversation/ConversationSeparationAi.java`（结构化返回）、`rag/grammar/GrammarErrorIndexListener.java`

**本计划 = MVP。** 不做：Top2/3 自动铸、卡上多道微练、SM-2、把落库包装成 Agent Tool、无 LLM 字段拷贝 Factory、铸卡逻辑写进 `ConversationAnalysisServiceImpl`。

---

## File map（内聚优先）

| 文件 | 职责 |
|---|---|
| `backend/.../sql/DDL.sql` + `schema-test.sql` | `growth_card` 表 |
| `.../model/GrowthCard.java` | 实体 |
| `.../mapper/GrowthCardMapper.java` + `GrowthCardMapper.xml` | 持久化 |
| **`.../growth/GrowthCardDraft.java`** | LLM 生成结果：front / back |
| **`.../growth/GrowthCardStore.java`** | Gateway/Review 共用的落库与查询（无 Spring Web） |
| **`.../growth/GrowthCardScheduler.java`** | 自评 → status + nextDueAt |
| **`.../growth/GrowthCardMintAssistant.java`** | `@AiService`：只生成 Draft，不落库 |
| **`.../growth/GrowthCardMintGateway.java`** | `mintAfterAnalysis`：generate → 显式 persist |
| **`.../growth/GrowthCardMintRequestedEvent.java`** + `GrowthCardMintListener.java`** | AFTER_COMMIT 触发 Gateway |
| `.../growth/GrowthCardReviewService.java` | 今日列表、grade、collect |
| `.../controller/GrowthCardController.java` + `dto/growth/*` | REST |
| `ConversationAnalysisServiceImpl` | **仅**发事件 + detail 读状态 |
| `frontend/...` | 类型、Deck、Home、详情状态/收藏 |

包内依赖方向：

```text
Listener → Gateway → MintAssistant.generate → Draft
                   ↘ Store.persistNewOrGet → Mapper
ReviewService / Controller → Store + Scheduler
```

---

## 数据约定

**表 `growth_card`：** 同前（`card_id`, `user_id`, `type`, `status`, `next_due_at`, `front`, `back`, `source_analysis_id`, `source_ref`, `evidence_json`, 时间戳）。  
唯一键：`UNIQUE(user_id, source_analysis_id, type, source_ref)`。

**自评：** `again`→unfamiliar/+1d；`fuzzy`→fuzzy/+3d；`good`→mastered、`next_due_at=null`。

**LLM 铸卡（habit）：** 助手输入 = Top1 `ActionCardDto` 摘要；返回 `GrowthCardDraft`；Gateway 再 `store.persistNewOrGet`（type/sourceRef 由 Gateway 决定）。**禁止**把报告字段原样当 front/back 跳过 LLM。

**vocab：** Gateway 在同一次 mint 中 Store 直写中英（MVP）。日后 vocab 也可改走 LLM generate。

---

### Task 1: DDL + 实体 + Mapper + Store

**Files:**
- Modify: `backend/src/main/resources/sql/DDL.sql`
- Modify: `backend/src/test/resources/schema-test.sql`
- Create: `backend/src/main/java/com/khankiddo/learning/model/GrowthCard.java`
- Create: `backend/src/main/java/com/khankiddo/learning/mapper/GrowthCardMapper.java`
- Create: `backend/src/main/resources/mapper/GrowthCardMapper.xml`
- Create: `backend/src/main/java/com/khankiddo/learning/growth/GrowthCardStore.java`
- Test: `backend/src/test/java/com/khankiddo/learning/growth/GrowthCardStoreTest.java`（可先单测 Store 幂等逻辑 + mock Mapper）

- [x] **Step 1: DDL**

```sql
CREATE TABLE IF NOT EXISTS `growth_card`
(
    `id`                  BIGINT AUTO_INCREMENT PRIMARY KEY,
    `card_id`             VARCHAR(64)  NOT NULL,
    `user_id`             BIGINT       NOT NULL,
    `type`                VARCHAR(16)  NOT NULL COMMENT 'habit|vocab',
    `status`              VARCHAR(16)  NOT NULL COMMENT 'unfamiliar|fuzzy|mastered',
    `next_due_at`         DATE                  DEFAULT NULL,
    `front`               TEXT         NOT NULL,
    `back`                TEXT         NOT NULL,
    `source_analysis_id`  VARCHAR(64)           DEFAULT NULL,
    `source_ref`          VARCHAR(128) NOT NULL,
    `evidence_json`       TEXT                  DEFAULT NULL,
    `created_at`          DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at`          DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY `uk_card_id` (`card_id`),
    UNIQUE KEY `uk_user_source` (`user_id`, `source_analysis_id`, `type`, `source_ref`),
    INDEX `idx_user_due` (`user_id`, `status`, `next_due_at`)
) ENGINE = InnoDB COMMENT ='成长卡';
```

H2：`schema-test.sql` 同步（无 ENGINE/COMMENT）。

- [x] **Step 2: Mapper 方法**

`insert`、`findByCardIdAndUserId`、`findByUserSource`、`findDueByUserId`、`updateReview`、`findHabitByAnalysis`（`type='habit' AND source_analysis_id=?`）。

- [x] **Step 3: `GrowthCardStore`**

```java
@Component
@RequiredArgsConstructor
public class GrowthCardStore {
    private final GrowthCardMapper mapper;

    /** 幂等：已存在则返回 existing，否则 insert 新卡（unfamiliar, nextDueAt=today） */
    public GrowthCard persistNewOrGet(long userId, String type, String front, String back,
                                      String sourceAnalysisId, String sourceRef, String evidenceJson) { ... }

    public List<GrowthCard> listDue(long userId, LocalDate today) { ... }
    public Optional<GrowthCard> findOwned(String cardId, long userId) { ... }
    public GrowthCard updateReview(String cardId, long userId, String status, LocalDate nextDueAt) { ... }
}
```

- [x] **Step 4: 测试 Store 幂等 + compile**

```bash
./mvn.sh -q -Dtest=GrowthCardStoreTest test
```

- [x] **Step 5: Commit**

```bash
git add backend/src/main/resources/sql/DDL.sql backend/src/test/resources/schema-test.sql \
  backend/src/main/java/com/khankiddo/learning/model/GrowthCard.java \
  backend/src/main/java/com/khankiddo/learning/mapper/GrowthCardMapper.java \
  backend/src/main/resources/mapper/GrowthCardMapper.xml \
  backend/src/main/java/com/khankiddo/learning/growth/GrowthCardStore.java \
  backend/src/test/java/com/khankiddo/learning/growth/GrowthCardStoreTest.java
git commit -m "$(cat <<'EOF'
feat: add growth_card persistence store

EOF
)"
```

---

### Task 2: Scheduler + GrowthCardTools（Agent Tool）

**Files:**
- Create: `backend/src/main/java/com/khankiddo/learning/growth/GrowthCardScheduler.java`
- Create: `backend/src/main/java/com/khankiddo/learning/growth/GrowthCardTools.java`
- Test: `backend/src/test/java/com/khankiddo/learning/growth/GrowthCardSchedulerTest.java`
- Test: `backend/src/test/java/com/khankiddo/learning/growth/GrowthCardToolsTest.java`（对齐 `GrammarLearningDbToolsTest`：校验 `@ToolMemoryId` 首参）

- [x] **Step 1: Scheduler 测试与实现**（again/fuzzy/good → 状态与日期，同 design §4.2）

```java
public final class GrowthCardScheduler {
    public record ReviewResult(String status, LocalDate nextDueAt) {}
    public ReviewResult apply(String grade, LocalDate today) {
        return switch (grade) {
            case "again" -> new ReviewResult("unfamiliar", today.plusDays(1));
            case "fuzzy" -> new ReviewResult("fuzzy", today.plusDays(3));
            case "good" -> new ReviewResult("mastered", null);
            default -> throw new IllegalArgumentException("unknown grade: " + grade);
        };
    }
}
```

- [x] **Step 2: 写 Tools 测试（结构约束）**

参考 `GrammarLearningDbToolsTest`：每个 `@Tool` 方法首参必须是 `@ToolMemoryId Long userId`。

- [x] **Step 3: 实现 `GrowthCardTools`**

```java
@Component("growthCardTools")
@RequiredArgsConstructor
public class GrowthCardTools {
    private final GrowthCardStore store;

    @Tool(
            name = "persist_growth_card",
            value = "将一张成长卡（练习闪卡）持久化到当前用户卡包。"
                    + "在已根据分析上下文写好 front/back 后调用。"
                    + "同一 sourceAnalysisId+type+sourceRef 重复调用不会重复插入。"
    )
    public String persistGrowthCard(
            @ToolMemoryId Long userId,
            @P(name = "type", description = "habit 或 vocab") String type,
            @P(name = "front", description = "正面：提示/中文/习惯问题") String front,
            @P(name = "back", description = "背面：英文目标说法或要点") String back,
            @P(name = "sourceAnalysisId", description = "来源分析 ID") String sourceAnalysisId,
            @P(name = "sourceRef", description = "幂等键，如 habit:FAM_WORD_FORM 或 vocab:0") String sourceRef) {
        GrowthCard card = store.persistNewOrGet(
                userId, type.trim().toLowerCase(), front, back,
                sourceAnalysisId, sourceRef, null);
        return "ok cardId=" + card.getCardId() + " type=" + card.getType();
    }
}
```

可选第二 Tool（MVP 可不做，预留聊天挂载）：

```java
@Tool(name = "list_due_growth_cards", value = "列出当前用户今日待复习成长卡摘要")
public String listDueGrowthCards(@ToolMemoryId Long userId) { ... }
```

- [x] **Step 4: 测试通过并 Commit**

```bash
./mvn.sh -q -Dtest=GrowthCardSchedulerTest,GrowthCardToolsTest test
git commit -m "$(cat <<'EOF'
feat: add growth card agent tools and scheduler

EOF
)"
```

---

### Task 3: GrowthCardMintAssistant + Gateway

**Files:**
- Create: `backend/src/main/java/com/khankiddo/learning/growth/GrowthCardMintAssistant.java`
- Create: `backend/src/main/java/com/khankiddo/learning/growth/GrowthCardMintGateway.java`
- Create: `backend/src/main/java/com/khankiddo/learning/growth/GrowthCardMintContextBuilder.java`（可选，避免 Gateway 膨胀）
- Test: `backend/src/test/java/com/khankiddo/learning/growth/GrowthCardMintGatewayTest.java`（mock Assistant / Store）

- [x] **Step 1: AiService 接口**

对齐 `GrammarStatsAssistant` 的 wiring：

```java
@AiService(
        wiringMode = AiServiceWiringMode.EXPLICIT,
        chatModel = "openAiChatModel",
        tools = {"growthCardTools"})
public interface GrowthCardMintAssistant {

    String SYSTEM = """
            你是 Khan Kiddo 知识卡片助手：根据用户消息中的任务与材料，生成可复习的正反面知识卡片。
            （具体 type / 是否落库 / sourceRef 由 userMessage 指定，不写死在 system。）
            """;

    @SystemMessage(SYSTEM)
    String mintCard(@MemoryId Long userId, @UserMessage String userMessage);
}
```

若项目 `@AiService` 需要 `streamingChatModel`，与 `GrammarStatsAssistant` 保持同样属性；铸卡用非流式 `chatModel` 即可。

- [x] **Step 2: ContextBuilder**

输入：`userId`, `analysisId`, 已算好的 `ActionCardDto topHabit`。  
输出给模型的 `mintBrief` 字符串（结构化文本即可），例如：

```text
analysisId: ...
sourceRef: habit:{habitKey}
headline: ...
why: ...
evidence:
- original: ... | suggestion: ...
practiceTarget: ...
请生成一张 habit 成长卡并 persist。
```

- [x] **Step 3: Gateway**

```java
@Service
@RequiredArgsConstructor
public class GrowthCardMintGateway {
    private final GrowthCardMintAssistant assistant;
    private final GrowthCardStore store;
    private final HabitCardScorer habitCardScorer; // 或委托 ConversationAnalysis 侧只读组装
    // ... mappers / summary parser

    public void mintAfterAnalysis(Long userId, String analysisId) {
        // 1. 加载分析归属与 items / chineseExpressions
        // 2. score → topHabit；若 null 跳过 habit
        // 3. 若已有 habit 卡则跳过 LLM
        // 4. assistant.mintCard(userId, brief)
        // 5. vocab：对每个 chineseExpression → store.persistNewOrGet(... type=vocab ...)
    }

    public void retryMint(Long userId, String analysisId) {
        mintAfterAnalysis(userId, analysisId);
    }
}
```

**内聚约束：** 组装 HabitScore 的代码若必须读 items，可把「只读组装」做成 Gateway 私有方法，或抽 `GrowthCardAnalysisSupport` 仍放在 `growth` 包；**不要**在 `ConversationAnalysisServiceImpl` 里调 Assistant。

- [x] **Step 4: Gateway 单测**

- topHabit null → 不调 assistant，仍可写 vocab  
- 已有 habit 卡 → 不调 assistant  
- topHabit 存在且无卡 → 调 assistant 一次  

- [x] **Step 5: Commit**

```bash
git commit -m "$(cat <<'EOF'
feat: add growth card mint assistant and gateway

EOF
)"
```

---

### Task 4: 异步事件接线（分析侧极薄）

**Files:**
- Create: `backend/src/main/java/com/khankiddo/learning/growth/GrowthCardMintRequestedEvent.java`
- Create: `backend/src/main/java/com/khankiddo/learning/growth/GrowthCardMintListener.java`
- Modify: `ConversationAnalysisServiceImpl.persistAnalysis` — **仅** `publishEvent`

- [x] **Step 1: Event + Listener**

```java
public record GrowthCardMintRequestedEvent(Long userId, String analysisId) {}

@Component
@RequiredArgsConstructor
public class GrowthCardMintListener {
    private final GrowthCardMintGateway gateway;

    @TransactionalEventListener(phase = TransactionPhase.AFTER_COMMIT)
    public void onMintRequested(GrowthCardMintRequestedEvent event) {
        Thread.startVirtualThread(() -> {
            try {
                gateway.mintAfterAnalysis(event.userId(), event.analysisId());
            } catch (Exception ex) {
                log.error("成长卡铸卡失败 analysisId={}", event.analysisId(), ex);
            }
        });
    }
}
```

- [x] **Step 2: persistAnalysis**

在分析 insert 成功后（无论有无 grammar items）一律：

```java
eventPublisher.publishEvent(new GrowthCardMintRequestedEvent(userId, analysisId));
```

- [x] **Step 3: Commit**

```bash
git commit -m "$(cat <<'EOF'
feat: trigger async growth card mint after analysis persist

EOF
)"
```

---

### Task 5: ReviewService + REST + 详情状态

**Files:**
- Create: `.../growth/GrowthCardReviewService.java`
- Create: `.../controller/GrowthCardController.java`
- Create: `.../dto/growth/GrowthCardDto.java` 等
- Modify: `ConversationAnalysisDetailDto` + `getDetail`（只查 Store，不铸卡）

- [x] **Step 1: ReviewService**

`listToday` / `grade` / `collect` — 全部 `SecurityUtils.requireUserId()`；`collect` 调 `store.persistNewOrGet`（与 Tool 同路径）。

- [x] **Step 2: Controller**

```
GET  /api/growth-cards/today
POST /api/growth-cards/{cardId}/grade   body: { grade }
POST /api/growth-cards/collect
POST /api/growth-cards/mint/{analysisId}  → gateway.retryMint
```

- [x] **Step 3: Detail 字段**

```java
private String habitGrowthMintStatus; // pending|ready|failed|none
private GrowthCardDto habitGrowthCard;
```

判定：有 habit 卡 → ready；topHabit==null → none；无卡且 createdAt&lt;30s → pending；否则 failed。

- [x] **Step 4: 测试 + Commit**

```bash
./mvn.sh -q test
git commit -m "$(cat <<'EOF'
feat: expose growth card review APIs and mint status on analysis detail

EOF
)"
```

---

### Task 6: 前端 Deck 组件（复习页复用；首页不接线）

**Files:**
- Create: `frontend/src/types/growthCard.ts`
- Create: `frontend/src/api/growthCard.ts`
- Create: `frontend/src/components/growth/GrowthFlashcardDeck.vue`
- HomeView：保持 DEMO 展示，**不**拉 `/today`、不接自评

- [x] **Step 1: API 类型** — `GrowthCard`、`fetchTodayGrowthCards`、`gradeGrowthCard`

- [x] **Step 2: `GrowthFlashcardDeck`** — `vue3-flashcards`；翻面后「不会/模糊/会了」→ grade；用 `--kk-*`（供后续复习页）

- [x] **Step 3: HomeView** — 始终 DEMO 自动演示；登录态也不改成练习

- [ ] **后续（另开任务）:** 独立「成长卡复习 / 复盘」页面接线 Deck + `/today`

---

### Task 7: 分析详情 — 状态 / 重试 / 收藏

**Files:**
- Modify: `AnalysisDetailView.vue`、`conversation.ts` 类型
- Modify: 句卡或 Fan — 「收成成长卡」→ `collect`

- [x] **Step 1:** pending 轮询；failed 重试 `POST .../mint/{id}`；ready 展示摘要  
- [x] **Step 2:** 原卡/中文表达收藏走同一 collect API  
- [x] **Step 3:** build + Commit

```bash
git commit -m "$(cat <<'EOF'
feat: analysis detail growth mint status and collect

EOF
)"
```

---

### Task 8: 文档收尾

- [x] 确认 design §6 与本 plan 一致  
- [x] `learning-loop-gaps` 顶部注明 P0 由成长卡吸收  
- [x] Commit docs

---

## Spec coverage

| Spec | Task |
|---|---|
| LLM 生成练习卡 | Task 3 Assistant |
| Assistant 只生成、Gateway 显式落库 | Task 3 |
| Gateway 唯一入口 | Task 3–4 |
| 异步不堵 Stage3 | Task 4 |
| Top1 只 1 张 habit | Assistant prompt + Gateway 跳过已存在 |
| vocab / 收藏同 Store | Task 1/3/5 |
| 今日到期 API + 调度 | Task 2/5 |
| 复习 Deck 组件（首页不接） | Task 6；独立复习页后续 |
| 详情 pending/retry | Task 5/7 |

## 风险

1. **Draft 无效：** Gateway 校验 front/back 非空再落库；否则标 failed 可重试。  
2. **无 API Key：** mint 失败不影响分析；详情 `failed` + 重试。  
3. **耗时：** 异步；勿合并进 Stage3 直至实测可接受。
