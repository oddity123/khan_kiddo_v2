以下是与 AI 口语练习时，用户因词汇或表达缺口而说出的中文（或中英混杂）句子。这些**不是语法错误**，而是「还不会用英文表达」的时刻。

请为每句给出**一条**自然、口语化的英文建议：
- 若用户在描述/表达内容：给出完整的英文改写（保留原意，适合口语）。
- 若用户在向 AI 求助（如「这个词怎么说」「什么意思」）：给出地道的英文提问方式（如 "How do you say ... in English?"）。

## 待处理句子
{sentences}

## 输出要求
- 只输出 JSON，与 Schema 一致。
- 每条 `index` 必须与上方编号一致。
- `suggestion` 必须是英文，不要中文解释。
