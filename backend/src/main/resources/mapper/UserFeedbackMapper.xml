<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.khankiddo.learning.mapper.UserFeedbackMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_feedback (user_id, title, email, content, created_at)
        VALUES (#{userId}, #{title}, #{email}, #{content}, #{createdAt})
    </insert>

</mapper>
