{
  "type": "object",
  "properties": {
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "kind": {
            "type": "string",
            "enum": ["chinese", "expression"],
            "description": "chinese=中文词汇缺口；expression=英文 unnatural 短对立"
          },
          "index": {
            "type": "integer",
            "description": "与输入列表编号一致（从 1 开始）"
          },
          "focusPhrase": {
            "type": "string",
            "minLength": 1,
            "description": "闪卡正面短焦点（chinese=中文目标；expression=不自然短片段）；同场勿与已输出项近义重复；无法短对立时勿输出该条"
          },
          "suggestion": {
            "type": "string",
            "minLength": 1,
            "description": "闪卡背面短对应（chinese=英文；expression=地道短说法）；同场勿近义重复"
          },
          "reason": {
            "type": "string",
            "minLength": 1,
            "description": "短中文说明：为何抽这个 / 学什么 / 为何这样改（每条保留项必填）"
          }
        },
        "required": [
          "kind",
          "index",
          "focusPhrase",
          "suggestion",
          "reason"
        ],
        "additionalProperties": false
      },
      "description": "可积累闪卡；跳过的 index 不出现；同场去重后的条目"
    }
  },
  "required": ["items"],
  "additionalProperties": false
}
