# 口语自然度综合评分配置（修改后重启生效）
# 权重键使用 ProblemType 枚举名（与 model.enums.ProblemType 一致）
app:
  performance-scoring:
    base-score: 98
    min-score: 45
    max-score: 98
    # penalty = density-multiplier * (1 - exp(-density-decay * weightedDensity))
    density-decay: 0.22
    density-multiplier: 34
    # 密度/占比分母平滑：effectiveSentences = totalSentences + k，缓解极短对话过度扣分
    density-smoothing-k: 2.0
    # 单句加权扣分上限，避免一句多错过度拉低分数
    max-weighted-penalty-per-sentence: 6.0
    # 含 FATAL 级错误句子的额外扣分系数（与 ProblemType.getErrorLevel 对齐）
    severe-sentence-penalty: 4.0
    # 含自然度类错误句子的额外扣分系数（仅作用于 naturalness 维度）
    naturalness-sentence-penalty: 5.0
    # 未知错误类型默认权重
    default-type-weight: 1.0

    type-weights:
      INCOMPLETE: 3.5
      CHINGLISH: 3.2
      COLLOCATION: 3.0
      UNNATURAL: 2.8
      STRUCTURE: 2.8
      WORD_CHOICE: 2.4
      TENSE: 2.2
      AGREEMENT: 2.1
      PREPOSITION: 1.9
      CLAUSE: 2.4
      FORMAL: 1.8
      VOCABULARY: 1.8
      WORD_FORM: 1.6
      PRONOUN: 1.5
      ARTICLE: 1.2
      PLURAL: 1.2
      REDUNDANCY: 1.1
      TONE: 1.4
      COMPARISON: 1.4

    dimensions:
      naturalness:
        weight-in-overall: 0.40
        types:
          - CHINGLISH
          - COLLOCATION
          - UNNATURAL
          - WORD_CHOICE
          - REDUNDANCY
          - TONE
          - FORMAL
      accuracy:
        weight-in-overall: 0.25
        types:
          - TENSE
          - AGREEMENT
          - PREPOSITION
          - STRUCTURE
          - CLAUSE
          - PLURAL
          - ARTICLE
          - PRONOUN
          - WORD_FORM
          - COMPARISON
      fluency:
        weight-in-overall: 0.20
        types:
          - INCOMPLETE
          - REDUNDANCY
      lexical:
        weight-in-overall: 0.15
        types:
          - VOCABULARY
          - WORD_CHOICE
          - COLLOCATION
