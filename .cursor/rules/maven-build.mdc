---
description: 终端 / Agent 执行 Maven 时必须使用本项目 JDK（Temurin 21），禁止直接调用裸 mvn。
alwaysApply: true
---

# Maven 构建（khan_kiddo_v2）

本项目使用 **Java 21**（Temurin）。系统默认可能是 JDK 8，直接运行 `mvn` 会失败或编译错误。

## 必须遵守

1. 在 **`/Users/oddity/workspace/khan_kiddo_v2`** 目录下执行构建（先 `cd` 到该目录）。
2. 使用项目根目录包装脚本，**不要**直接运行 `mvn`：

```bash
cd /Users/oddity/workspace/khan_kiddo_v2
./mvn.sh -q compile
./mvn.sh -q test
./mvn.sh -q package -DskipTests
```

3. 若无法使用脚本，必须显式设置 `JAVA_HOME` 再调用 Maven：

```bash
export JAVA_HOME="/Library/Java/JavaVirtualMachines/temurin-21.jdk/Contents/Home"
mvn -q compile
```

## 验证

`./mvn.sh -version` 输出中 `Java version` 应为 **21.x**（Eclipse Adoptium / Temurin）。
