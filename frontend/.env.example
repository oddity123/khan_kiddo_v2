# 前端环境变量模板
# 用法：cp .env.example .env.local 后按需修改（.env.local 已在 .gitignore 中）

# 开发环境 API 地址
# 留空：走 Vite 代理 /api -> http://localhost:<PORT>（PORT 来自仓库根 .env，默认 8080）
# 非空：直连该地址，例如 http://localhost:8080
VITE_API_BASE_URL=

# 可选：覆盖 Vite 代理目标端口（未设置时使用根目录 .env 的 PORT）
# VITE_DEV_API_PORT=8080
