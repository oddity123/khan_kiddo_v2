# 前端环境变量模板
# 用法：cp .env.example .env.local 后按需修改（.env.local 已在 .gitignore 中）

# 开发环境 API 地址
# 留空：走 Vite 代理 /api -> http://localhost:8081（需先启动 backend）
# 非空：直连该地址，例如 http://localhost:8081
VITE_API_BASE_URL=
